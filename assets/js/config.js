/* =============================================================================
 * config.js — 배포 설정 (커밋되는 파일)
 * -----------------------------------------------------------------------------
 * sharedUrl: 공유 서버(Cloudflare Worker) 주소.
 *   여기에 배포한 워커 URL을 넣고 커밋하면, 친구들은 사이트에서
 *   "공유 비밀번호"만 입력하면 공유 기업 목록을 보고 직접 분석·저장할 수 있습니다.
 *   예: sharedUrl: 'https://naver-proxy.<계정>.workers.dev'
 *   (워커에 KV(STORE)·비밀번호(STORE_KEY) 설정 필요 — serverless/README.md 참고)
 * =========================================================================== */
window.SITE_CONFIG = {
  sharedUrl: 'https://falling-math-382.ccsunkr.workers.dev'
};
